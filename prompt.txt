"I need a simple Admin Page to create new tours easily. It should have:

Dynamic Form: Fields for all columns in the package tours table.

Add Days Feature: A way to add multiple days to the itinerary without writing JSON manually.

Submit Button: When clicked, it should use supabase.from('package tours').insert() to save the new tour.

Security: For now, just make it a hidden route, but later we will protect it with a password."