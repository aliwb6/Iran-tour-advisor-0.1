Subject: Remove Custom Trip Page and Enhance AI Assistant Logic

Please apply the following changes to the project:

1. Navigation & Route Cleanup:

Remove Custom Trip: Completely remove the "Custom Trip" page, its route, and its link from the Navigation Bar and Footer.

AI Responsibility: The logic for "Building a Personalized Trip" should now be handled exclusively by the AI Assistant.

2. AI Assistant Enhancement:

Database Context: Update the AI Assistant's prompt/context so that it knows about our available Tour Packages (from the tours table) and Approved Local Guides.

Functionality: When a user asks for a personalized or custom trip, the AI should suggest elements from existing packages or recommend connecting with a specific local guide.

3. User Registration Logic (Tourist vs. Guide):

Role Selection: On the Sign-up page, implement a toggle or radio button to choose between "Tourist" (default) and "Local Guide".

Guide Onboarding: If a user registers as a Guide, they must be redirected to a Profile Creation form (Bio, City, Specialty).

Approval System: Profiles for Guides must have an is_approved status set to false by default and should not appear in the "Local Guides" section until approved by an admin.

4. UI Consistency:

Package Cards: Ensure the "Tour Packages" page shows a grid of cards (as per the dark cinematic design) instead of redirecting to a single detail page immediately.

Guide Cards: Show only one template card for now, which will dynamically populate once Guides are approved