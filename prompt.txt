Subject: User Roles Implementation (Tourist/Guide) and UI Flow Correction

Please implement the following features and fixes:

1. Registration Flow (Tourist vs. Guide):

Sign-up Page: Create a registration form that defaults to the "Tourist" role.

Role Toggle: Add a clear option at the bottom of the form: "Are you a Local Guide? Register as a Guide."

Tourist Access: Tourists should be able to sign up easily with their email and get immediate access to the site.

Guide Onboarding: When "Guide" is selected, after registration, redirect them to a "Create Your Profile" page where they can enter their bio, expertise, and city.

Admin Approval Logic: Add a hidden is_approved boolean field (default: false) to the Guide's profile in Supabase. Guides' profiles should NOT be visible on the site until an admin manually changes this to true.

2. Tour Packages UI Correction:

List View vs. Detail View: Stop the auto-redirect to a single package.

Tour Index Page: Create a grid of "Tour Cards" based on the provided screenshots. Each card must show the thumbnail, title, price, and duration.

Navigation: Only navigate to the "Package Detail" page when a user clicks on a specific card.

3. Guides Section Cleanup:

Remove all hardcoded guide profiles.

Keep only one template for the Guide Card.

Dynamic Fetching: Set up the Guides page to fetch and display only profiles from the profiles table where role === 'guide' AND is_approved === true.

4. Supabase Integration:

Update the profiles table schema to include role (enum: tourist, guide) and is_approved (boolean).