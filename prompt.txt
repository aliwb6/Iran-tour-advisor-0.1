I want to update the "Featured Tours" section on my homepage to "Featured Packages" (پکیج‌های منتخب). Please perform the following tasks:

1. UI Update:

Change the section heading from "سفرهای منتخب" to "پکیج‌های منتخب".

Ensure the styling matches the cinematic and premium aesthetic of Iran Tour Advisor, specifically referring to the card layout in "Screenshot_20260509_231124.jpg".

2. Logic Update (Data Fetching):

Modify the fetching logic to retrieve tour packages from the database (PostgreSQL) ordered by their Rating or Score in descending order.

Limit the display to the top 3 or 4 highest-rated packages.

If a package doesn't have a rating, treat it as 0 or place it at the end of the list.

3. Component Integration:

Use the existing TourCard component but ensure it displays the rating star/score if available.

The data should be fetched server-side (if using Next.js App Router) or via an API call that specifically queries for top-rated tours.

Please update the code in my homepage file (likely page.tsx or Home.tsx) to reflect these changes.