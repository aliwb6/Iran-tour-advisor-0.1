"I have successfully set up my Supabase database with a tours table containing columns like title, slug, description, price, image_url, duration, and location. I've also enabled RLS (Public Read).

Please update the project with these requirements:

Data Integration: Replace the current Mock Data in the Tours and Home pages with real data fetched from my Supabase tours table using the @supabase/supabase-js client.

Missing Pages: Create the /destinations page and ensure it dynamically lists locations.

Search Logic: Implement the /search page so it actually filters the tours table in Supabase.

UX: Add Loading Skeletons for the data-fetching states and implement Fallback Images for any missing thumbnails.

SEO: Ensure each tour's detail page uses its Supabase data (title and description) for SEO metadata.

Please use the supabaseClient.js for all database calls and provide the updated code for the fetching logic."